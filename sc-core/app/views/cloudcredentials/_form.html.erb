<%= form_with(model: cloudcredential, local: true) do |form| %>
  <% if cloudcredential.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cloudcredential.errors.count, "error") %> prohibited this cloudcredential from being saved:</h2>

      <ul>
      <% cloudcredential.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :uuid %>
    <%= form.text_field :uuid, id: :cloudcredential_uuid %>
  </div>

  <div class="field">
    <%= form.label :properties %>
    <%= form.text_area :properties, id: :cloudcredential_properties %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
