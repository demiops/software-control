<%= form_with(model: authcredential, local: true) do |form| %>
  <% if authcredential.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(authcredential.errors.count, "error") %> prohibited this authcredential from being saved:</h2>

      <ul>
      <% authcredential.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :uuid %>
    <%= form.text_field :uuid, id: :authcredential_uuid %>
  </div>

  <div class="field">
    <%= form.label :properties %>
    <%= form.text_area :properties, id: :authcredential_properties %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
